{
  "name": "@arkhe-aio/public",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "validate:deploy-config": "node scripts/validate-cloudflare-config.mjs",
    "validate:public-surface": "node scripts/validate-public-surface.mjs",
    "deploy": "npm run validate:deploy-config && npm run validate:public-surface && wrangler deploy",
    "dev": "wrangler dev",
    "check": "npm run validate:deploy-config && npm run validate:public-surface && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
